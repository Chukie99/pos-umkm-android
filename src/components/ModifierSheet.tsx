import React from 'react'
import { View } from 'react-native'

/** Modifier picker — REMOVED per user request (produk tidak pakai varian). */
export default function ModifierSheet() {
  return <View />
}
